/**
 * RegularUser class.
 * Represents a regular user in the system.
 * Extends the {@link User} class.
 */
public class RegularUser extends User {
    public RegularUser(int id, String name) {
        super(id, name);
    }

    // RegularUser-specific methods
    // ...
}